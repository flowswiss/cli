//go:build cloudbit

package build

const (
	Name        = "cloudbit"
	Description = "cloudbit is a command-line interface for managing the Cloudbit platform."
	Version     = "v2.0.0-beta.1"

	DefaultEndpoint = "https://api.cloudbit.ch/"
)
